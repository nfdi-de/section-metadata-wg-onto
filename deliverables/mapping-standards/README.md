The document managed in this directory is written in file [`index.qmd`](index.qmd) in Markdown syntax using [quarto](https://quarto.org/) so both HTML and PDF can be generated from one source.

Preview HTML:

    quarto preview